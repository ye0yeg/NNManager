<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:orientation="vertical">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/text_is_hot"
            style="@style/QueryOrderTab"
            android:drawableRight="@drawable/refresh_logo_inactive"
            android:text="@string/search_order_is_hot"/>

        <TextView
            android:id="@+id/text_most_expensive"
            style="@style/QueryOrderTab"
            android:drawableRight="@drawable/refresh_logo_active"
            android:text="@string/search_order_price"/>


    </LinearLayout>

    <in.srain.cube.views.ptr.PtrFrameLayout
        android:id="@id/standard_refresh_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ListView
            android:id="@+id/list_cust"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:divider="@null"
            android:dividerHeight="@dimen/spacing_normal"
            android:scrollbars="none"/>

    </in.srain.cube.views.ptr.PtrFrameLayout>
</LinearLayout>